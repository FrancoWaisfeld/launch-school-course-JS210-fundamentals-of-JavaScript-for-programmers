function logOddNumbers(num) {
  for (let count = 1; count <= num; count++) {
    if (count % 2 === 1) {
    }
    console.log(count);
  }
}

logOddNumbers(19);

/*

explicit rules:
- write a function that takes a positive integer as an argument
- log all the odd numbers from 1 to the passed in number (inclusive)
- log all numbers on a seperate line

- 


*/
